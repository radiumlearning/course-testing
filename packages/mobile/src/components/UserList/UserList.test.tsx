import 'react-native';
import React from 'react';
import {render, waitFor} from 'src/test/testing-library';
import UserList from '.';
import * as Mocks from '../../mocks';

describe('UserList', () => {
  it('UserList displays all users', () => {});
  it('UserList displays `no data` text', () => {});
  it('UserList displays fetching status', () => {});
});
