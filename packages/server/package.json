{
  "name": "@rn-testing-class/server",
  "version": "1.0.0",
  "main": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon --watch '*.ts' --exec 'ts-node' src/index.ts",
    "test": "TZ=UTC jest",
    "test-c": "yarn jest --ci --colors --runInBand --reporters=default --reporters=jest-junit --coverage --verbose"
  },
  "dependencies": {
    "@rn-testing-class/lib": "*",
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "nodemon": "^2.0.15",
    "ts-node": "^10.4.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13"
  }
}
